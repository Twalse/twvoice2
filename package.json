
{
  "name": "twvoice",
  "version": "2.1.2",
  "description": "Premium Voice & Video Chat for Gamers",
  "main": "server.js",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=index.js --format=esm --minify --loader:.tsx=tsx --loader:.ts=ts --platform=browser --define:process.env.NODE_ENV=\\\"production\\\"",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "esbuild": "^0.24.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "lucide-react": "^0.474.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "TwVoice Team",
  "license": "MIT"
}
